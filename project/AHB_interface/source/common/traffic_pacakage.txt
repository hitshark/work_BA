1、对事务包装模块，若是字节，半字或者是字传输，则数据存放的空间是由new操作符动态分配的，如果是突发传输，则数据的首地址作为参数传递。没有使用new分配的空间；
2、若要使总线宽度可配置，则在事务包装模块需要改变；还不太清楚一些实现细节，看库里面的代码；
3、在该模块中实现了对事务的最后处理，即从response_fifo中读出的事务；

对于打印信息，写操作完成是在从模块中打印出来的，而读操作则是在主设备中打印；对于突发操作，则先根据返回值判断是否操作成功，然后再显示相应的信息；
当然，打印的顺序是可以改变的；